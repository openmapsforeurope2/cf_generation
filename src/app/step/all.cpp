#include <app/step/all.h>

namespace app {
namespace step{

	void AllTrans::onCompute(  bool verbose  )
{
/*
	RailwayInitialization rwInitialization;
	rwInitialization.init();
	RoadInitialization roadInitialization;
	roadInitialization.init();

	//rwInitialization.onCompute(verbose);
	roadInitialization.onCompute(verbose);


	RailwaySelection rwSelection;
	rwSelection.init();	


	//rwSelection.onCompute(verbose);
	


	RailwayModeleConversion rwModeleConversion;
	rwModeleConversion.init();


	RoadSelection roadSelection;
	roadSelection.init();
	roadSelection.onCompute(verbose);


	//rwModeleConversion.onCompute(verbose);

	RailwayEdgeMatching rwEdgeMatching;
	rwEdgeMatching.init();

	//rwEdgeMatching.onCompute(verbose);
	*/

}

}
}